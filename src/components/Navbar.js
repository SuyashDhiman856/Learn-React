export default function Navbar({style, name, width, height})
{
    return (
        <>
            <div style={style}>
                <h1>{name}</h1>
                <img src="" width={width} height={height}/>
            </div>
        </>
    )   
}
